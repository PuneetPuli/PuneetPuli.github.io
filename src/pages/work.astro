---
const projects = [
  {
    title: "Katanamu - Formal Proof Checker",
    meta: "Dafny -- Formal Verification -- Logic (Fall 2025)",
    blurb:
      "Formally verified natural deduction proof checker for propositional logic (and, or, implication). Type-safe formula representation, 8 inference rules, 70+ verified tests, and constraints that prevent circular reasoning.",
    links: [{ label: "GitHub", href: "https://github.com/PuneetPuli" }],
  },
  {
    title: "Amazon Stock Prediction Pipeline",
    meta: "Python -- scikit-learn -- pandas (Spring 2025)",
    blurb:
      "End-to-end ML pipeline using HuberRegressor + SGDClassifier for direction prediction. Feature engineering: momentum, technical signals, lag variables, smoothing.",
    links: [{ label: "GitHub", href: "https://github.com/PuneetPuli" }],
  },
  {
    title: "MIPS Pipeline Simulator & Branch Predictor",
    meta: "C -- JavaScript -- Computer Architecture (2024)",
    blurb:
      "5-stage pipelined MIPS in C + branch prediction simulator (Bimodal, GShare, Local History, Two-Level Adaptive) with a web visualizer.",
    links: [{ label: "GitHub", href: "https://github.com/PuneetPuli" }],
  },
  {
    title: "Multi-threaded Receipt Processor",
    meta: "Java -- Concurrency -- Data Structures (2023)",
    blurb:
      "Processes 10,000+ transactions concurrently using producer–consumer architecture and custom thread-safe data structures.",
    links: [{ label: "GitHub", href: "https://github.com/PuneetPuli" }],
  },
  {
    title: "Programming Language Interpreter",
    meta: "OCaml -- Languages / Compilers (2024)",
    blurb:
      "Interpreter with lexer/parser → AST → evaluator. Supports higher-order functions, closures, recursion, and robust runtime errors.",
    links: [{ label: "GitHub", href: "https://github.com/PuneetPuli" }],
  },
];

const experience = [
  {
    title: "AIThinkers - Software Engineering Intern",
    meta: "Jun 2025 - Aug 2025 (Remote)",
    bullets: [
      "Built a CV pipeline (YOLOv8, Python) for vehicle detection in aerial imagery (CARPK).",
      "Converted 90,000+ annotations to YOLO format (pandas).",
      "Optimized training/inference and presented reproducible metrics to stakeholders.",
    ],
  },
];
---

<main>
  <div class="kicker">Work</div>
  <h1>Systems. Proof. Signal.</h1>
  <p class="small">
    Low-level + formally verified systems. Java --- C --- Dafny --- OCaml --- Coq 
    <br />
    <a href="https://github.com/PuneetPuli" target="_blank" rel="noreferrer">GitHub</a>
     ---
    <a href="mailto:puli.puneet@gmail.com">puli.puneet@gmail.com</a>
  </p>

  <hr />

  <h2>Selected Projects</h2>
  <div class="stack">
    {projects.map((p) => (
      <section class="item">
        <div class="row">
          <h3>{p.title}</h3>
          <div class="meta">{p.meta}</div>
        </div>
        <p>{p.blurb}</p>
        <div class="links">
          {p.links.map((l) => (
            <a href={l.href} target="_blank" rel="noreferrer">{l.label}</a>
          ))}
        </div>
      </section>
    ))}
  </div>

  <hr />

  <h2>Experience</h2>
  <div class="stack">
    {experience.map((e) => (
      <section class="item">
        <div class="row">
          <h3>{e.title}</h3>
          <div class="meta">{e.meta}</div>
        </div>
        <ul>
          {e.bullets.map((b) => <li>{b}</li>)}
        </ul>
      </section>
    ))}
  </div>
</main>

<style>
  .stack { display: grid; gap: 18px; }
  .item { padding: 14px 0; border-top: 1px solid var(--line); }
  .item:first-child { border-top: 0; padding-top: 0; }
  .row { display: flex; justify-content: space-between; gap: 12px; flex-wrap: wrap; align-items: baseline; }
  h3 { margin: 0; font-size: 1.05rem; }
  .meta { color: var(--muted); font-size: .92rem; }
  ul { margin: 10px 0 0; padding-left: 18px; }
  li { margin: 6px 0; }
  .links { display: flex; gap: 12px; flex-wrap: wrap; margin-top: 10px; }
</style>

